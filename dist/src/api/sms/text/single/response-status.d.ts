import { Status } from "../../../status";
export declare class SmsTextSingleResponseStatus {
    groupId: string;
    groupName: string;
    id: string;
    name: Status;
    description: string;
    action: string;
    constructor(groupId: string, groupName: string, id: string, name: Status, description: string, action: string);
}
